# NotionSnap — 开发路线图

> 最后更新：2026-05-27 | 当前版本：v0.6.1

## 产品定位

**替代 Notion Web Clipper（不稳定）和 Copy to Notion（付费），做免费、可靠、智能的网页剪藏工具。**

核心原则：先把"提取质量"做到行业第一，再把"保存体验"做到零摩擦，最后才谈智能组织。

---

## 已完成（v0.1.0 → v0.6.1）

### 内容提取引擎

- **7 个独立解析器**：微信公众号（15 层降级 + 50+ 噪声清理）、Twitter/X 线程、GitHub README & Issues、知乎问答、小红书笔记、Medium、Substack（含自定义域名）
- **30+ 站点智能适配**：按域名分类匹配最优选择器和噪声规则（博客/新闻/科技/加密/通用）
- **11 种 Notion block 类型**：段落、H1-H3 标题、引用、无序/有序列表、代码块（自动语言检测）、分隔线、图片、视频（embed + video block）、表格
- **视频平台页面解析**：YouTube / Bilibili / TikTok / Douyin 页面识别 + 元数据提取 + embed block
- **文章内嵌视频提取**：iframe/a 标签检测视频嵌入 → embed block，支持 YouTube/Bilibili/TikTok/Douyin/微信视频号
- **视频占位符**：CDN 不可达时自动降级为蓝色加粗文字标记（Twitter/微信公众号/小红书）
- **富文本内联样式**：加粗、斜体、删除线、链接、内联代码、11 种文字颜色
- **元数据提取**：作者、发布时间、摘要、网站名称、语言、封面图、字数（中英文）
- **通用降级**：自动识别 article/main/post-content 语义标签，纯文本分块兜底
- **安全网**：block 类型三层验证防御（内容脚本过滤 → SW 安全网 → SW 内容字段验证）

### 保存系统

- **两阶段保存**：Phase 1 秒级落库（文字 + external URL）+ Phase 2 后台替换（下载 → Notion S3 托管）
- **SW 中断续传**：浏览器关闭后，Service Worker 重启自动恢复 Phase 2 任务
- **保存去重**：URL 级别 5 秒去重，防止重复点击创建多条记录
- **3 层错误恢复**：网络波动自动重试（3 次退避）、API 限流自动等待、Token 过期自动刷新
- **分批写入**：100 blocks/批，递归追加避免 SW 休眠

### 目标选择

- **自动记忆**：每个网站记住上次保存到哪个页面/数据库，下次自动选中
- **自动字段匹配**：保存到数据库时按属性名/类型自动匹配 9 个元数据字段
- **保存预设**：5 色 pill 按钮一键切换目标，最多 15 个
- **渐进式搜索**：4 路并行加载、300ms 防抖、滚动分页（上限 30 条）
- **永久保存目标**：用户保存过的页面不受 Notion API 100 条限制
- **页面列表缓存**：打开弹窗秒出下拉数据，5 分钟后台静默刷新

### 交互方式

- Popup 面板（预览编辑 → 选择目标 → 保存）
- 右键菜单一键保存
- 全局快捷键 `Ctrl+Shift+S`
- 保存进度实时反馈（分块进度 + badge 状态 + toast 通知）

### 多账号 & UI

- **多工作区**：OAuth 绑定多个 Notion 账号，独立存储（页面列表/预设/历史/缓存）
- **双主题**：Raycast（暗色金属 + 紫红渐变）、Vercel（Geist 字体 + 1px 边框）
- **保存历史**：按日期分组，支持复制链接/打开 Notion/重试失败
- **元数据编辑**：保存前展开编辑 9 个字段，修改直接生效

### CWS 上架准备

- 产品文档、营销话术、CWS listing 文案 ✅
- 5 张 1280×800 场景截图 ✅
- UI 全景截图 ✅
- 尚未提交审核（需 Google 开发者账号）

---

## 待做

### P0 — Chrome Web Store 提交

提交审核：注册 Google 开发者账号 → 上传素材 → 填写隐私政策 → 提交。半天工作量。

### P1 — 图片上传成功率提升

CDN 防盗链图片（微信、少数派等）Phase 2 替换会失败。方案选项：
- A：部署代理服务器绕过 Referer 检查（增加运营成本）
- B：接受 external URL 保留，不做强制替换
- C：用 Chrome 扩展权限直接从页面获取图片 blob（绕过 Referer）

### P1 — 更多网站适配

高频候选：Mirror.xyz、Substack 外 Newsletter 平台、BBC/CNN 等新闻站点精调、小红书图文混排优化。按需求逐个添加。

### P2 — 多浏览器适配

Firefox MV3（2024 已支持）、Edge（Chromium 兼容）。核心代码不动，测试 + 打包 + 各商店上架。1-2 天。

### P2 — 文章内嵌视频 ✅ (v0.6.1)

已完成：YouTube/Bilibili/TikTok/Douyin 页面识别 + embed block，文章内嵌 iframe 自动提取，CDN 不可达视频蓝色加粗占位符（Twitter/微信/小红书）。

### P2 — 技术债

| 事项 | 工作量 | 说明 |
|------|--------|------|
| tests/verify.js 扩充端到端测试 | 半天 | 模拟完整保存流程 |
| SW 终止恢复边界情况完善 | 半天 | 边缘 case 覆盖 |
| Popup 大页面提取性能优化 | 1 小时 | 提取与渲染分离 |

### 远期愿景

- AI 摘要/翻译（用户自带 API Key）
- RSS → Notion 自动存档
- 批量保存（收藏夹/标签页一次性导入）
- 相似内容智能去重
- 移动端 Safari 扩展

---

## 开发纪律

1. 每个版本必须跑 `npx vite build` 通过
2. 每次发版更新 README.md 和 CHANGELOG.md
3. 大功能先在 Plan Mode 出方案再动手
4. 纯原生 JS，零运行时依赖不变
5. Service Worker 兼容性约束（不用 async/await，全部 Promise 链）
6. 不要引入复杂配置到 popup 主流程——保持一键操作，高级设置收敛到 settings

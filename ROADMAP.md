# NotionSnap — 整体开发路线图

> 最后更新：2026-05-24 | 当前版本：v0.5.1

## 产品定位

**替代 Notion Web Clipper（不稳定）和 Copy to Notion（付费），做免费、可靠、智能的网页剪藏工具。**

核心原则：先把"提取质量"做到行业第一，再把"保存体验"做到零摩擦，最后才谈智能组织。用户在乎的是存出来的内容能不能直接用，操作是否顺手。

## 版本路线图

### v0.3.x — 稳定与打磨（已完成）

| 版本 | 功能 | 状态 |
|------|------|------|
| v0.3.6 | 代码审计收尾 + 图片提取兼容 + 设置页重构 + CSS 死代码清理 | ✅ |
| v0.3.7 | 右键菜单保存 + 全局快捷键 | ✅ |
| v0.3.8 | 错误恢复增强（网络重试可视化、大页面分批进度） | ✅ |
| v0.3.9 | Chrome Web Store 上架准备（截图、描述、隐私政策） | 📋 |

### v0.4.x — 效率提升（当前）

| 版本 | 功能 | 状态 |
|------|------|------|
| v0.4.0 | 保存预设（pill 一键切换目标页面，最多 15 个，彩色标签） | ✅ |
| v0.4.0 | 保存历史记录面板（按日期分组、复制链接、打开 Notion、重试失败项） | ✅ |
| v0.4.0 | File Upload 图片上传（二进制下载 → Notion S3，解决防盗链） | ✅ |
| v0.4.0 | 并行下载优化（所有图片同时下载，上传排队） | ✅ |
| v0.4.1 | 图片上传修复（Notion API Gateway POST + FormData + Auth header） | ✅ |
| v0.4.1 | 搜索优化（即时本地渲染 + API 合并去重、层级排序、类型标签） | ✅ |
| v0.4.1 | 初始加载覆盖扩展（3 种排序去重，page_size 100） | ✅ |
| v0.4.1 | 默认下拉简洁化（最近保存 + 5 数据库 + 5 一级页面） | ✅ |
| v0.4.1 | 右键/快捷键保存 fallback 到最近页面 | ✅ |

### v0.5.0 — 自定义保存字段（已完成）

| 功能 | 状态 |
|------|------|
| 自定义保存字段（字段映射编辑器，URL/作者/发布时间/关键词 → Notion properties） | ✅ |
| 内容关键词提取写入 Notion properties（中文 N-gram 频率分析） | ✅ |

### v0.5.1 — 自动字段匹配 + 体验优化（已完成）

| 功能 | 状态 |
|------|------|
| 自动字段匹配（保存到数据库时加载 schema，按属性名/类型自动匹配 9 个元数据字段） | ✅ |
| 首页"更多"可展开元数据面板（标题下方编辑 9 个字段，保存时修改生效） | ✅ |
| 页面列表缓存（秒开下拉框，5 分钟后台静默刷新） | ✅ |
| 下拉框两类结构（最近保存 5 个 + 所有页面 10 个/可展开 20） | ✅ |
| 自动选中上次保存的页面/数据库为默认目标 | ✅ |
| 增强元数据提取（9 字段覆盖中英文网页，中文关键词自动提取） | ✅ |
| 移除手动字段映射配置（被自动匹配替代） | ✅ |

### v0.6.0 — 发布与分发

| 功能 | 工作量 | 优先级 |
|------|--------|--------|
| Chrome Web Store 上架 | 半天 | P0 |
| Firefox / Edge 适配 | 1~2 天 | P2 |

### v0.7.0 — 核心体验优化（需先调研）

| 功能 | 工作量 | 优先级 | 说明 |
|------|--------|--------|------|
| 搜索功能优化 | 调研 1 天 + 开发 1~2 天 | P1 | 调研 Notion 官方插件及其他竞品的搜索实现方案，对比差距后优化 |
| 图片上传性能优化 | 调研 1 天 + 开发 1~2 天 | P1 | 当前图片上传导致保存耗时过长（几秒到几十秒），调研竞品方案后重构 |
| 多网站适配 | 调研 1 天 + 开发 2~3 天 | P1 | 目前主要适配公众号，Twitter/博客等第三方网站保存失败，需调研通用网页提取方案 |

### 远期愿景

- AI 智能摘要/翻译（用户自带 API Key）
- RSS 转 Notion
- 视频/音频内容提取
- 智能去重（相似内容检测）

## 优先级矩阵

```
P1（当前）    Chrome Web Store 上架（等待商店图片素材就绪）

P2（有余力）  内容关键词提取、前后对比预览、批量保存、多浏览器适配
              搜索完整性（部分页面 API 不返回，需排查 Integration 权限）
```

## 技术债

| 事项 | 工作量 | 说明 |
|------|--------|------|
| tests/verify.js 功能测试扩充 | 半天 | 端到端测试（模拟完整保存流程） |
| Service Worker 终止恢复完善 | 半天 | 已做 save_state 跟踪，需覆盖更多边缘情况 |
| 搜索覆盖率 | 待排查 | 部分一级页面 API 三种排序均不返回，可能是 Integration 权限问题 |
| Popup 性能优化 | 1 小时 | 大页面提取延迟，提取 + 渲染分离 |

## 开发纪律

1. 每个版本必须跑 `npx vite build` 通过
2. 每次发版更新 README.md 变更日志
3. 大功能先在 Plan Mode 出方案再动手
4. 纯原生 JS，零运行时依赖不变
5. Service Worker 兼容性约束（不用 async/await，全部 Promise 链）
6. 不要在 popup 中引入复杂配置——保持主流程一键操作，高级设置收敛到 settings panel

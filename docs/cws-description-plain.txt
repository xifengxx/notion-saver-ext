⚡ 把任何网页，一秒变成整洁的 Notion 笔记
Save any webpage to Notion in one click.

NotionSnap 是 Notion 官方 Clipper 的最佳免费替代品 —— 更稳定、更智能、专为中文内容深度优化。
The free, open-source Notion clipper that actually works — especially for Chinese content.

🤔 你是不是也遇到过？Does this sound familiar?
- 用 Notion Web Clipper 保存文章，结果只得到一个空白页
  Saved a page with Notion Web Clipper, got a blank page.
- 公众号链接复制到 Notion，过几天打开已被删除
  Pasted a WeChat article link, opened it days later — gone.
- GitHub README 粘到 Notion 里格式全乱，代码块变成纯文本
  Copied a GitHub README into Notion, formatting destroyed.
- Twitter 看到好内容只能截屏，事后根本搜不到
  Screenshotted a good Twitter thread because nothing else worked.

NotionSnap 解决上述所有问题。
NotionSnap fixes all of the above. Not "yet another clipper" — the one you actually need.

🛠️ 专用解析引擎 · Custom Parsers
市面上大多数剪藏工具用一套逻辑处理所有网页。NotionSnap 为每种网站单独编写了解析函数。
Most clippers use one generic logic for everything. NotionSnap has dedicated parsers per site type.
✅ 多层降级提取 + 噪声清理，覆盖多种文章格式和页面结构
   Multi-layer extraction with noise cleanup, handles diverse page structures.
✅ 富文本完整保留：加粗、斜体、代码块、表格、引用，语言自动检测
   Full rich text: bold, italic, code blocks, tables, quotes, auto language detection.
✅ 元数据自动抓取：标题、作者、发布时间、摘要、封面图
   Auto metadata extraction: title, author, date, summary, cover image.
✅ 内容安全网：极端情况下正则兜底提取纯文本
   Last-resort fallback: regex plain-text extraction when all else fails.

🌐 30+ 站点智能适配 · 30+ Site Adapters
按域名分类匹配最优提取策略，覆盖博客平台、新闻媒体、科技媒体、加密媒体等主流内容站点。未适配网站有通用语义提取兜底。
Genre-based extraction across blogs, news, tech, and crypto media. Universal semantic fallback for unsupported sites.

🧱 11 种 block 类型 · 11 Block Types
段落 / Paragraph · H1-H3 标题 / Headings · 引用 / Quote · 无序列表 / Bulleted list · 有序列表 / Numbered list · 代码块（自动语言检测）/ Code block · 分隔线 / Divider · 图片 / Image · 视频（embed + video）/ Video · 表格 / Table · 内联样式 / Inline styles（加粗/斜体/删除线/链接/代码/11 色）

🖱️ 三种保存方式 · Three Ways to Save
💡 Popup 面板 / Panel：点图标 → 预览编辑 → 保存
💡 右键菜单 / Right-click：页面右键 → 一键保存
💡 全局快捷键 / Shortcut：Ctrl+Shift+S

🛡️ 稳定可靠 · Built to Last
✅ 3 层错误恢复：网络波动自动重试，API 限流自动等待
   3-layer error recovery: auto-retry on network errors, auto-wait on rate limits.
✅ SW 中断续传：浏览器关闭后下次启动自动恢复，不丢数据
   Service Worker resume: picks up where it left off, even after browser restart.
✅ 保存进度透明：popup 实时进度 + badge 状态 + toast 通知
   Transparent progress: real-time block count, badge status, toast notifications.

🎨 双主题 · Dual Themes
🌙 Raycast 暗色 / Dark：金属质感 + 紫红渐变
☀️ Vercel 黑白 / Light：Geist 字体 + 1px 边框

🔒 隐私优先 · Privacy First
✅ 数据直达 Notion，不经过任何第三方服务器
   Content goes directly from browser to Notion API — zero third-party servers.
✅ OAuth 2.0 认证，不存储你的密码
   OAuth 2.0 auth — we never see your password.
✅ 100% 开源，代码在 GitHub 公开可审计
   100% open source. Audit the code yourself.
✅ 仅必要权限：activeTab、storage、scripting、notifications、contextMenus
   Minimal permissions only.

🚀 快速开始 · Quick Start
1. 安装扩展，点击工具栏图标 / Install, click the toolbar icon
2. 连接到 Notion 完成 OAuth 授权 / Connect to Notion via OAuth
3. 打开任意网页，点图标 → 保存 / Open any page, click icon → save
4. 也可用右键菜单或快捷键 Ctrl+Shift+S / Or right-click or Ctrl+Shift+S
